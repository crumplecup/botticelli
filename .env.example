# Boticelli Configuration
# Copy this file to .env and fill in your values

# ============================================================================
# LLM API Keys
# ============================================================================

# Gemini API Key (required for Gemini backend)
# Get yours at: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Anthropic API Key (optional, for future Anthropic backend support)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API Key (optional, for future OpenAI backend support)
# OPENAI_API_KEY=your_openai_api_key_here

# ============================================================================
# Database Configuration (Optional)
# ============================================================================

# PostgreSQL connection (only needed if you want to use the --save flag)
#
# Option 1: Component-based (recommended - compose from parts)
# This allows you to keep credentials separate and change hosts easily
DATABASE_USER=boticelli
DATABASE_PASSWORD=your_password_here
DATABASE_HOST=localhost      # Optional: defaults to localhost
DATABASE_PORT=5432            # Optional: defaults to 5432
DATABASE_NAME=boticelli       # Optional: defaults to boticelli
#
# Option 2: Complete URL (alternative - takes precedence if set)
# Format: postgres://username:password@host:port/database
# DATABASE_URL=postgres://boticelli_user:password@localhost:5432/boticelli
#
# Setup instructions:
# 1. Install PostgreSQL: https://www.postgresql.org/download/
# 2. Create user: createuser -P boticelli (set password when prompted)
# 3. Create database: createdb -O boticelli boticelli
# 4. Run migrations: diesel migration run
# 5. Configure credentials above

# ============================================================================
# Logging Configuration
# ============================================================================

# Rust logging level
# Options: trace, debug, info, warn, error
# Can also target specific modules, e.g., boticelli::narrative=debug
RUST_LOG=info

# ============================================================================
# OpenTelemetry Configuration (Optional)
# ============================================================================

# OpenTelemetry exporter backend
# Options: "stdout" (development, human-readable), "otlp" (production)
# Default: stdout
OTEL_EXPORTER=stdout

# OTLP endpoint for traces and metrics (required if OTEL_EXPORTER=otlp)
# Default: http://localhost:4317
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
#
# For docker-compose stack: http://localhost:4317
# For remote collector: https://your-collector:4317
#
# Setup with docker-compose:
# 1. Start observability stack: docker-compose up -d
# 2. Set OTEL_EXPORTER=otlp
# 3. Access Jaeger UI: http://localhost:16686

# ============================================================================
# Application Settings (Advanced)
# ============================================================================

# Maximum retries for failed API calls (not yet implemented)
# MAX_RETRIES=3

# Request timeout in seconds (not yet implemented)
# REQUEST_TIMEOUT=30
