# TUI Justfile Recipe Enhancement - COMPLETE

**Date**: 2025-12-15
**Status**: Complete - Enhanced justfile recipe with full feature support

## Summary

Updated the `just tui` recipe to provide a comprehensive, user-friendly way to launch the new TuiApp with all features enabled and proper validation.

## Changes Made

### Enhanced `tui` Recipe

**File**: `justfile` lines 845-888

Added comprehensive bash script that:

1. **API Key Validation**: Checks for `ANTHROPIC_API_KEY` with helpful error message
2. **Directory Setup**: Creates `narratives/` directory if missing
3. **Feature Documentation**: Displays at startup:
   - Chat with Claude (MCP tool integration)
   - Narrative tools available (create, validate, list, load)
   - 5 tools ready to use
   - Async tool execution with real-time updates
4. **Controls Guide**: Shows all keybindings:
   - Tab for view switching (Chat → Narratives → Editor → Settings)
   - Shift+Tab for backward cycling
   - Ctrl+Q and Ctrl+C to quit
5. **Debug Instructions**: Shows how to enable debug logging
6. **Proper Feature Flags**: Runs with `--features anthropic`
7. **Logging Configuration**: Sets appropriate RUST_LOG defaults

### Supporting Recipes

**`tui-debug`** (lines 895-909):
- Launch with full debug logging enabled
- Same validation and setup as main recipe
- Debug level for TUI, MCP client, and MCP core

**`tui-example`** (lines 912-926):
- Run the chat_with_mcp example
- Debug logging for development
- Same validation as main recipe

**`tui-chat`** (line 891-892):
- Maintained for backward compatibility
- Old interface using botticelli-chat binary

## Usage

### Basic Usage
```bash
export ANTHROPIC_API_KEY=sk-ant-...
just tui
```

### With Debug Logging
```bash
export ANTHROPIC_API_KEY=sk-ant-...
RUST_LOG=botticelli_tui=debug,botticelli_mcp_client=debug just tui
```

Or use the debug recipe:
```bash
just tui-debug
```

### Run Example
```bash
just tui-example
```

## Verification

Compilation checks pass:
```bash
cargo check --bin tui
cargo check -p botticelli_tui --all-targets
```

Both succeed with zero TUI-specific warnings.

## Features Enabled

When you run `just tui`, you get:

1. Full MCP integration with 5 tools:
   - `echo` - Simple echo for testing
   - `create_narrative` - Create narratives from TOML
   - `validate_narrative` - Validate narrative structure
   - `list_narratives` - List available narratives
   - `load_narrative` - Load narrative from file

2. View switching with Tab:
   - Chat mode (conversation with Claude)
   - Narrative Browser (navigate narratives)
   - Narrative Editor (edit narratives)
   - Settings (placeholder for future)

3. Async tool execution with real-time UI updates via channel

4. Proper error handling and validation

## Integration with TUI Composable Architecture

This recipe launches the new TuiApp created in the composable architecture work:

- Uses `TuiApp::new(driver)` factory pattern
- Views are composable and pluggable
- Commands flow through centralized dispatch
- Events handled by EventHandler
- MCP updates via tokio channel

See `TUI_COMPOSABLE_ARCHITECTURE_COMPLETE.md` for full architecture details.

## Next Steps

Ready for testing:

1. Set your API key: `export ANTHROPIC_API_KEY=sk-ant-...`
2. Run: `just tui`
3. Test view switching with Tab
4. Try asking Claude to use narrative tools
5. Test all keybindings

For development/debugging:
- Use `just tui-debug` for verbose logging
- Use `just tui-example` to test example
- Use `RUST_LOG=trace just tui` for maximum detail

---

Generated by Claude Code - Botticelli TUI Justfile Recipe Enhancement
